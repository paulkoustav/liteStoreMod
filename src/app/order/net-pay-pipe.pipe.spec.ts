import { NetPayPipePipe } from './net-pay-pipe.pipe';

describe('NetPayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NetPayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
